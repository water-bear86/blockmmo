{
  "name": "runechain",
  "version": "0.1.0",
  "description": "A tiny Soulslike MMO demo with a real SHA-256 proof-of-work blockchain mechanic.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node scripts/verify_core_runtime.js && node scripts/verify_turnbased_rpg.js && node scripts/verify_chainwell_authority.js && node scripts/verify_account_binding.js && node scripts/verify_rune_spend.js && node scripts/verify_gold_cosmetics.js && node scripts/verify_presence_modes.js"
  },
  "engines": {
    "node": ">=16"
  }
}
